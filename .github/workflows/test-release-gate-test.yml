name: Test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "ok=true"
          test -f src/_cli_version.py
